# Upstream sync dry run — 2026-07-13

Per Loop.md §8 "Fork maintenance" and Phase-0.5 backlog item 7. **DRY RUN
ONLY — nothing merged.** The actual merge is a reviewed weekend task.

## Compared commits

| | commit |
|---|---|
| local `main` | `b2d15fdfa` (feat(finance): dedicated-bot support …) |
| merge base | `4281151ae` (test(gateway): cover effective context budget) |
| `upstream/main` (NousResearch/hermes-agent) | `7b5ba2054` (fix(desktop): resync fallback editor after config reload) |
| divergence | upstream is **67 commits ahead**; we carry the Finance feature set on top of the merge base |

`upstream` remote configured: `https://github.com/NousResearch/hermes-agent.git`.

## Dry-run merge result

`git merge --no-commit --no-ff upstream/main` on branch
`sync/upstream-2026-07-13`: **exit 0 — ZERO conflicts.** The Finance work
(trader/, finance_proxy, web/desktop finance views) does not collide with any
of the 67 upstream commits. Merge was aborted after inspection (dry run).

Upstream footprint: 240 files, +5,242 / −23,804 lines.
Change mix: 31 fix · 7 feat · 4 test · 4 refactor · 4 chore · 1 docs.

## Triage highlights (impact on Finance boundaries)

- `fix(api): run transport/stream lifetime` series — gateway API server;
  no overlap with the finance service (separate process/port).
- `fix(web): preserve declared providers in model writes` — web app config
  page; FinancePage untouched.
- desktop Fallback Models editor changes — desktop settings area; finance
  view untouched (our own route/view files).
- `fix(kanban)/fix(approval)` — dashboard plugins/actions; the finance
  proxy router is additive and conflict-free.
- Nothing touches `hermes_cli/web_server.py` in a way that conflicts with
  our two additive hooks (finance proxy include_router at app creation, and
  `HERMES_DASHBOARD_EXTRA_HOSTS` in the Host allowlist) — verified by the
  clean merge.

## Recommendation

Safe to merge at the next weekend window. Process for the REAL merge:
1. `git checkout sync/upstream-2026-07-13 && git merge upstream/main`
2. Run: trader suite (`cd trader && uv run --no-sync pytest`), web
   typecheck/build, desktop typecheck, `docker compose config -q`,
   then rebuild the docker image and click through /finance.
3. Human reviews the diff summary; merge to `main`; delete the sync branch.
4. Record the outcome in a new dated report in this directory.

## Follow-ups

- None blocking. Branch `sync/upstream-2026-07-13` left in place for the
  reviewed merge.
