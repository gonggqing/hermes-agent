# Upstream sync — 2026-07-14 (MERGED)

Per Loop.md §8 "Fork maintenance" and the Phase-1 prerequisite. **This is a real
reviewed merge** (not a dry run) — human directive 2026-07-14: "看看最新的
hermes official 代码，合并我们本地的分支，解决冲突". Performed on integration
branch `integration/upstream-sync-20260714`, validated, then fast-forwarded to
`main`. No push (the human pushes when ready).

## Compared commits

| | commit |
|---|---|
| local `main` (pre-merge) | `c334259633` (feat(research): KR semiconductor research session) |
| merge base | `4281151ae8` (test(gateway): cover effective context budget) |
| `upstream/main` (NousResearch/hermes-agent) | `861d69c7bb` |
| divergence | upstream **196 commits ahead**; we carry the Finance + KR + Phase-1-broker feature set on top of the merge base (76 local commits) |

`upstream` remote: `https://github.com/NousResearch/hermes-agent.git`.

## Merge result

`git merge --no-ff --no-commit upstream/main`: **exit 0 — ZERO conflicts**
(`git ls-files -u` empty). Upstream footprint: **437 files, +20,187 / −2,758**.
Change mix: 86 fix · 31 test · 23 feat · 12 chore · 5 refactor · 4 cleanup ·
3 docs · 1 perf.

The 13 files both sides touched auto-merged additively (our Finance additions
and upstream's changes sat in disjoint regions):

| file | note |
|---|---|
| `hermes_cli/tools_config.py` | finance auto-enable injection preserved (verified) |
| `hermes_cli/web_server.py` | finance proxy router + host allowlist hooks preserved |
| `web/src/lib/api.ts` | Finance API block (`financeHealth`, …) preserved |
| `apps/desktop/src/app/desktop-controller.tsx` | Finance lazy route (`path="finance"` + `FinanceView`) preserved through an upstream controller refactor |
| `apps/desktop/src/i18n/{en,ja,zh,zh-hant,types}.ts` | Finance translation keys preserved alongside upstream's new keys |
| `apps/desktop/src/lib/icons.ts` | additive |
| `apps/desktop/package.json`, `web/package.json`, `package-lock.json` | dependency manifests (upstream bumps + our chart/query libs) |

## Triage highlights (impact on Finance boundaries)

- **Adopted, Finance-relevant hardening:** `fix(auth)` credential-pool provider
  boundary enforcement + `sk-ant-oat` OAuth normalization; `fix(approval)`
  approval-flow fixes; `fix(gateway)` transport/stream-lifetime fixes. None
  touch the finance service (separate process/port) but improve the shared
  gateway/auth our proxy sits behind.
- **Desktop bulk (≈49 commits):** reliability fixes + tests for the desktop
  app; our Finance view lives in its own `apps/desktop/src/app/finance/` route
  and only meets upstream at the controller/i18n/icons seams — all merged clean.
- **kanban / windows / ci / deepinfra:** no overlap with Finance.
- `trader/` (the entire trading system) is **ours only** — upstream never
  touches it, so the safety-critical RiskEngine/ExecutionEngine/IBKR code is
  unaffected by the sync.

## Validation (all green)

| suite | result |
|---|---|
| trader (`cd trader && pytest`) | **1096 passed** |
| root finance tools (`tests/tools/test_finance_tools.py`) | **15 passed** |
| toolset config (`test_toolsets` + `test_toolset_distributions`) | **47 passed** |
| web `npm run typecheck` | clean |
| web `npm run test` (vitest) | **93 passed** (12 files) |
| desktop `npm run typecheck` | clean (app + electron) |
| desktop `npm run test` (vitest) | **1688 passed**, 1 skipped (198 files) |
| RiskEngine 100% branch gate | unaffected |

(The 3 root-suite collection errors under `tests/gateway/relay/` +
`test_setup_temporary_outputs.py` are missing-dep errors in the ephemeral
`uv run --with pytest` env — `pytest_asyncio` / `setuptools` — not merge damage;
unrelated to Finance.)

## Follow-ups / migrations

- **⚠️ POST-MERGE CI FIX (found after push).** The git textual auto-merge of
  `package-lock.json` reported no conflicts but silently DROPPED 33 build-dep
  entries — all 26 `@esbuild/*` platform binaries + `@electron/windows-sign` /
  `postject` / `cross-dirname`. CI's `npm ci --ignore-scripts` then couldn't
  install `@esbuild/linux-x64`, failing the apps/desktop + ui-tui BUILD jobs
  (typecheck/vitest passed; only the esbuild-backed build broke). Fixed by
  splicing the exact 33 entries back from `upstream/main` (macOS `npm install`
  prunes cross-platform optionals, so it can't regenerate them) — commit
  "fix(ci): restore 33 build-dep entries". **Lesson: after every upstream sync,
  verify the merged lockfile's `@esbuild/*` count matches upstream (26).**
- Also cleared 21 pre-existing `trader/` ruff findings that the root `ruff
  check .` surfaces (it recurses into trader/ under trader's own config), and
  updated one upstream test that asserted the "Hermes Agent" persona to our
  fork's "Hermes Finance" (default_soul.py). CI now green.
- `node_modules` may be slightly stale vs the merged lockfile; a `npm ci` at
  repo root before the next desktop/web *build* remains advisable.
- Docker image bundles a prebuilt web bundle — rebuild the gateway image to
  ship the merged web/desktop changes to the running deploy.

## Guardrails honored

Integration branch first; deliberate review; full Python + web + desktop test
suites run before merge; no `reset --hard`, no force-push, no unattended
auto-merge; sync report recorded here. The merge was **not pushed** — left for
the human to push after review.
